# S0 — Bootstrap Rust e package aarch64

**Verdetto:** PASS  
**Data:** 2026-08-14  
**Sorgente:** `bdfd7e8b47e599fcfffd18dd94b5adc1d4308803`  
**CI host:** [run 31752382671](https://github.com/frenchfaso/termux-stacks/actions/runs/31752382671)

## Artefatti verificati

| Artefatto | Valore |
|---|---|
| source archive SHA-256 | `b9018f5749c10cc8d14d0075f4ec5bb5b9820d443e3a5c42d1d0ff1678d3b366` |
| `termux-packages` commit | `d1dafbcff908affb4e0aa38d66af93e5edb4a599` |
| package | `termux-stacks 0.0.1-alpha.0-0`, `aarch64` |
| `.deb` SHA-256 | `8b3b306154e6ccab67c212e3bbf03d67ea9e9353a6686a071e3a385f13e24f28` |
| `.deb` size | 133,784 byte |
| installed size | 405 KiB dichiarati dal package |
| ELF SHA-256 | `9f6236e9e1fa4810908240f1e20a148781dca98bc5809e286bf8cb6e0929f542` |
| ELF size | 353,136 byte, stripped, Android API 24, AArch64 |

Il source archive è stato creato dal commit pulito con prefix, senza directory
`.git`. La build nativa ha usato Rust/Cargo 1.97.1 e `Cargo.lock`; la crate
diretta è `libc 0.2.189`. L'ELF importa `flock@LIBC` e richiede soltanto
`libc.so` e `libdl.so`. Il package dichiara soltanto `termux-services`; non
contiene un secondo binario `termux-stacksd`, maintainer script o download al
momento di installazione/avvio/runtime.

Il primo package prodotto dal fallback on-device è stato rifiutato prima
dell'installazione perché il collector timestamp aveva incluso file estranei
modificati nel `$PREFIX` vivo. La recipe temporanea è stata ristretta a una
allowlist e il package finale, ispezionato prima di APT, contiene soltanto:

- `$PREFIX/bin/termux-stacks`;
- link alla licenza Apache-2.0;
- `termux-stacksd/{run,down,log/run}`.

## Device e risultati

Galaxy A15 (`SM-A155F`), Android 16/API 36, aarch64, filesystem F2FS;
`proot-distro 5.6.0`, `termux-services 0.13-1`, `runit 2.1.2-4`.

L'harness device v2 sul binario installato ha prodotto **24 PASS, 0 FAIL,
0 SKIP**. Ha coperto CLI, ELF, permessi, `flock` interoperabile, singleton,
recupero dopo TERM/KILL, package e stato runit disabilitato per default. Il
bundle raw con `SHA256SUMS` resta nell'area di evidenza del device; nel
repository si conserva questo record sintetico e privo di dati identificativi.

Il ciclo runit separato ha verificato:

| Caso | Esito |
|---|---|
| enable/start, socket, lock e log | PASS |
| secondo daemon rifiutato | PASS |
| down/up e recupero socket stale | PASS |
| SIGKILL e restart automatico con nuovo PID | PASS |
| disable finale, nessun daemon, `dpkg -V` pulito | PASS |

Il device è stato lasciato con `termux-stacksd` disabilitato. S0 non qualifica
ancora semantica OCI command, session registry, signal/tree-kill PRoot,
ownership durante install engine, manifest, SQLite o lifecycle applicativo:
questi sono rispettivamente S1–S5.
