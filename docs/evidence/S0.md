# S0 — Rust bootstrap and aarch64 package

- **Verdict:** PASS
- **Date:** 2026-08-14
- **Source:** `bdfd7e8b47e599fcfffd18dd94b5adc1d4308803`
- **Host CI:** [run 31752382671](https://github.com/frenchfaso/termux-stacks/actions/runs/31752382671)

## Verified artifacts

| Artifact | Value |
|---|---|
| source archive SHA-256 | `b9018f5749c10cc8d14d0075f4ec5bb5b9820d443e3a5c42d1d0ff1678d3b366` |
| `termux-packages` commit | `d1dafbcff908affb4e0aa38d66af93e5edb4a599` |
| package | `termux-stacks 0.0.1-alpha.0-0`, `aarch64` |
| `.deb` SHA-256 | `8b3b306154e6ccab67c212e3bbf03d67ea9e9353a6686a071e3a385f13e24f28` |
| `.deb` size | 133,784 bytes |
| installed size | 405 KiB declared by the package |
| ELF SHA-256 | `9f6236e9e1fa4810908240f1e20a148781dca98bc5809e286bf8cb6e0929f542` |
| ELF size | 353,136 bytes, stripped, Android API 24, AArch64 |

The source archive was created from the clean commit with a prefix and without
a `.git` directory. The native build used Rust/Cargo 1.97.1 and `Cargo.lock`;
the direct crate dependency is `libc 0.2.189`. The ELF imports `flock@LIBC`
and requires only `libc.so` and `libdl.so`. The package declares only
`termux-services`; it contains no second `termux-stacksd` binary, maintainer
scripts, or downloads during installation, startup, or runtime.

The first package produced by the on-device fallback was rejected before
installation because the timestamp collector had included unrelated files
modified in the live `$PREFIX`. The temporary recipe was restricted to an
allowlist, and the final package, inspected before APT, contains only:

- `$PREFIX/bin/termux-stacks`;
- a link to the Apache-2.0 license;
- `termux-stacksd/{run,down,log/run}`.

## Device and results

Galaxy A15 (`SM-A155F`), Android 16/API 36, aarch64, F2FS filesystem;
`proot-distro 5.6.0`, `termux-services 0.13-1`, `runit 2.1.2-4`.

Device harness v3 ran against the installed binary and produced **24 PASS,
0 FAIL, 0 SKIP**. It covered the CLI, ELF, permissions, interoperable `flock`,
singleton enforcement, recovery after TERM/KILL, the package, and the
default-disabled runit state. The raw bundle with `SHA256SUMS` remains in the
device evidence area; this repository retains the summarized record without
identifying data.

Identifiers from the latest regression run: `s0.sh`
`4f9f030df2468372f74493e1476545d1433387014f38e4ad76c1c3339b3ee67e`,
`lib.sh` `77052dcea8c8507008567a0d2c3938bde9784fbe56c75a668047558541ad23af`,
`SHA256SUMS` index
`6665413a13598c7cc3ce199912f7d396361e611f9defa7bc18a829598c7c6d35`.

The separate runit cycle verified:

| Case | Result |
|---|---|
| enable/start, socket, lock, and log | PASS |
| second daemon rejected | PASS |
| down/up and stale-socket recovery | PASS |
| SIGKILL and automatic restart with a new PID | PASS |
| final disable, no daemon, clean `dpkg -V` | PASS |

The device was left with `termux-stacksd` disabled. S0 does not yet qualify OCI
command semantics, the session registry, PRoot signal/tree-kill behavior,
ownership during engine installation, the manifest, SQLite, or the application
lifecycle; those belong to S1–S5 respectively.
